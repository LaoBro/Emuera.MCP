# I-14 阶段 C 待办事项

> 状态：已完成（Step 1-6 全部完成，System.Drawing.Common 依赖彻底移除）
> 日期：2026-07-03
> 目标：彻底移除 System.Drawing.Common NuGet 依赖

## 已完成

### Phase 1（前序工作）
- EmuFont 值类型（替代 Font）
- EmuFontStyle 值类型（替代 FontStyle）
- IImageContext/IBitmapImage/IBrush 抽象接口
- HeadlessImageContext/HeadlessBitmap/HeadlessBrush 存根实现
- FontFactory 返回 EmuFont
- StringStyle 完全迁移到 EmuColor/EmuFontStyle
- AConsoleDisplayPart/AConsoleDisplayNode 的 Color/ButtonColor 迁移到 EmuColor
- Config.ForeColor/BackColor/FocusColor/LogColor 迁移到 EmuColor
- Process.ScriptProc 的 Color/FontStyle 迁移到 EmuColor/EmuFontStyle
- Instraction.Child 的 Color/FontStyle 迁移到 EmuColor/EmuFontStyle

### Phase 2（已部分完成）
- **Step 1: DrawTo 签名重构** ✅ — 所有 DrawTo 方法从 `Graphics` 改为 `IImageContext`
  - AConsoleDisplayPart, AConsoleDisplayNode, ConsoleStyledString, ConsoleShapePart (3子类), ConsoleDisplayLine, ConsoleButtonString, ConsoleImagePart, ConsoleDivPart
- **Step 2: HeadlessStringMeasure 参数替换** ✅ — `GetDisplayLength` 参数从 `Font` 改为 `EmuFont`
- **Step 3: GraphicsImage stub 与调用方对齐** ✅ — 采用 `#if HEADLESS` 条件编译策略（非创建独立 HeadlessGraphicsImage 类）。HEADLESS stub 此前已存在但与调用方未对齐，导致 11 个构建错误，现已修复：
  - GraphicsImage HEADLESS stub 补 `Bitmap => null` 属性（供 CBGSETGRAPHG 等的 `g.Bitmap == null` 判断）
  - ASprite HEADLESS 补 `SpriteGetColor` 返回 `EmuColor.Black`（供 SPRITEGETCOLOR 脚本调用）
  - AppContents.CreateSpriteG stub 签名改为 `(string, GraphicsImage, EmuRectangle)`，匹配调用方传的 `Rectangle`（隐式转换）
  - Creator.Method.cs 4 处用 `#if HEADLESS` 分流：GGETPEN/GGETPENWIDTH/GGETBRUSH、GSETBRUSH、SPRITEMOVE、GraphicsSave
  - 验证：`dotnet build` 0 错误 0 警告，`run_all.py` 全 PASS（survival 18/18）
- **Step 4: Config 清理** ✅ — Config 体系从 `Color` 完全迁移到 `EmuColor`：
  - `ConfigItem.cs`: `ConfigItem<Color>` → `ConfigItem<EmuColor>`（ValueToString/TryParse 分支），`tryStringsToColor` 返回类型 `Color` → `EmuColor`，`Color.Black`/`Color.FromArgb` → `EmuColor.*`，补 `using MinorShift.Emuera.Primitives`
  - `ConfigData.cs`: 6 处 active + 4 处注释块 `new ConfigItem<Color>(..., Color.FromArgb(...))` → `new ConfigItem<EmuColor>(..., EmuColor.FromArgb(...))`，`GetConfigValueInERB` 中 `Color color = item.GetValue<Color>()` → `EmuColor color = item.GetValue<EmuColor>()`，补 `using MinorShift.Emuera.Primitives`
  - `Config.cs`: 6 处 `GetConfigValue<Color>` → `GetConfigValue<EmuColor>`（ForeColor/BackColor/FocusColor/LogColor/RikaiColorBack/RikaiColorText），`DefaultFont` 内部 `FontStyle.Regular` → `EmuFontStyle.Regular`
  - 验证：`dotnet build` 0 错误 0 警告，`run_all.py` 全 PASS（survival 18/18，4 个 suite 全 PASS）
- **Step 5: 清理 using 语句** ✅（基本完成）— 所有 HEADLESS 可见代码中的 `System.Drawing*` 引用已清除或包裹：

### Step 5 详细完成清单

#### Tier 1-2（机械替换，前序会话完成）
- `ConsoleStateData.cs`、`ArgumentBuilder.cs`、`PluginManager.cs`、`ConsolePrintManager.cs`、`EmueraConsole.cs`、`HtmlManager.cs`、`ConsoleDivPart.cs` — `using System.Drawing;` 包裹 `#if !HEADLESS`，类型替换为 Emu 系列
- `Shape.cs` — `BoxBorder` 类整段包裹 `#if !HEADLESS`
- `Utils.cs` — `LoadImage`/`MakeIconFromBmpFile` 包裹 `#if !HEADLESS`，`AddColorParam`/`AddColorParam4` 改为 `EmuColor`

#### Tier 3（需新类型 EmuRectangleF）
- `EmuRectangleF.cs` ✅ — 新建 `public readonly record struct`（float 版矩形），含双向隐式转换到 `System.Drawing.RectangleF`
- `ConsoleShapePart.cs` ✅ — `using System.Drawing;` 包裹 `#if !HEADLESS`；3 处 `new RectangleF(...)` → `new EmuRectangleF(...)`；字段/参数 `RectangleF` → `EmuRectangleF`

#### Tier 2（with 表达式处理 readonly 字段突变）
- `ConsoleImagePart.cs` ✅ — `using System.Drawing;` 包裹 `#if !HEADLESS`；`private readonly Rectangle destRect` → `EmuRectangle`；`Size spriteSize` → `EmuSize`；构造体内字段赋值改 `with` 表达式：`destRect = destRect with { X = -destRect.Width };`、`destRect = destRect with { Y = destRect.Y - destRect.Height };`

#### Tier 5（大文件深度重构）
- `EmuRectangle.cs` ✅ — 补 `IntersectsWith(EmuRectangle)` 方法（供 Creator.Method.cs 调用）
- `Creator.Method.cs` ✅（~7200 行，30 处改动）— HEADLESS 可见代码全部迁移到 Emu 类型：
  - `using System.Drawing;` 包裹 `#if !HEADLESS`（第 17-19 行）
  - 简单类型替换：`Color` → `EmuColor`（GetColor/GetBGColor/ColorFromName/GraphicsGetColor/GraphicsSetColor/GraphicsSetBrush/GraphicsSetPen/GraphicsClear/SpriteGetColor 等），`Point` → `EmuPoint`（GraphicsGetColor/GraphicsSetColor/GraphicsDrawGWithRotate/GraphicsDrawLine/SpriteSetPos/SpriteGetColor/GraphicsCreate/CBGSetGraphics/CBGSetCIMG/CBGSETButtonSprite 等），`Rectangle` → `EmuRectangle`（SpriteCreate/GraphicsFillRectangle/GraphicsDrawG/GraphicsDrawSprite/SpriteAnimeAddFrame 等）
  - 深度 GDI+ 方法体包裹 `#if HEADLESS return ...; #else ... #endif`：CheckfontMethod（InstalledFontCollection/FontFamily）、GraphicsSetFontMethod（Font/FontFamily/GraphicsUnit/FontStyle）、GraphicsDrawStringMethod.GetIntValue（Bitmap/Graphics/Font/MeasureString）、GraphicsGetTextSizeMethod.GetIntValue、MouseButtonMethod（PointToClient/ClientRectangle/MoveMouse）
  - `Color.FromName` + `color.A > 0` 检查 → `EmuColor.TryFromName(colorName, out var color)`
  - `readonly record struct` 字段突变用 `with` 表达式：`destRect = destRect with { X = p.X, Y = p.Y };`（GraphicsDrawSpriteMethod）

#### Tier 5（HEADLESS 存根改造）
- `GraphicsImage.cs` ✅ — HEADLESS stub 签名从 `System.Drawing.Rectangle`/`System.Drawing.Bitmap` 改为 Emu 类型：
  - `List<Tuple<ASprite, System.Drawing.Rectangle>> drawImgList` → `EmuRectangle`
  - `GDrawRectangle`/`GFillRectangle`/`GDrawCImg`/`GDrawG` 签名 `System.Drawing.Rectangle` → `EmuRectangle`
  - `public System.Drawing.Bitmap Bitmap => null;` → `public object Bitmap => null;`（调用方仅做 `== null` 判断）

#### Tier 4（纯 GDI+ 工具类整段包裹）
- `ImgUtils.cs` ✅ — 整个 `static class ImgUtils` 包裹 `#if !HEADLESS ... #endif`（`LoadImage` 返回 `Bitmap`，调用 `WebP.Load`/`new Bitmap(filepath)`）
- `WebPWrapper.cs` ✅ — `using System.Drawing;`/`using System.Drawing.Imaging;` 包裹 `#if !HEADLESS`；整个 `public sealed class WebP` 包裹 `#if !HEADLESS ... #endif`（含 Load/Decode/Encode/AdvancedEncode 等 Bitmap 操作）
  - `UnsafeNativeMethods`（P/Invoke）、枚举、结构体不依赖 System.Drawing，保持 HEADLESS 可见

#### Headless 自有源码清理
- `GlobalStatic.cs` ✅ — 移除 `using System.Drawing.Text;`；`Pfc` 属性类型从 `PrivateFontCollection` 改为新建存根 `HeadlessFontCollection`（`AddFontFile`/`Dispose` 空实现）；Reset 中 `new PrivateFontCollection()` → `new HeadlessFontCollection()`
- `Headless/HeadlessFontCollection.cs` ✅ — 新建存根类，`internal sealed class HeadlessFontCollection : IDisposable`，`#pragma warning disable CA1822`（存根方法故意保持实例方法签名以匹配调用方）
- `Program.cs` ✅ — 无需改动，`GlobalStatic.Pfc.AddFontFile(fontFile)` 调用自动适配新存根类型

#### B 类文件验证（已有 `#if HEADLESS`/`#else` 切分，`#else` 分支保留 System.Drawing）
- `AppContents.cs` ✅ — `#if HEADLESS` stub + `#else` 真实实现，`using System.Drawing;` 在 `#else` 内
- `ConstImage.cs` ✅ — 同上
- `CroppedImage.cs` ✅ — 同上
- `ConsoleStyledString.cs` ✅ — `using System.Drawing;` 在 `#if !HEADLESS` 内

#### 验证
- `dotnet build` 0 错误 0 警告
- `run_all.py` 全 PASS（survival 18/18，4 个 suite 全 PASS）

### Step 6 详细完成清单

#### Emu 类型隐式转换隔离（HEADLESS 编译时不引用 System.Drawing）
所有 `Emu*` 值类型的双向隐式转换到 `System.Drawing.*` 类型用 `#if !HEADLESS` 包裹（HEADLESS 构建无需这些转换，因调用方已统一用 Emu 类型）：
- `EmuColor.cs` ✅ — `EmuColor ↔ System.Drawing.Color` 双向隐式转换 + 注释包裹 `#if !HEADLESS`
- `EmuPoint.cs` ✅ — `EmuPoint ↔ System.Drawing.Point` 双向隐式转换包裹 `#if !HEADLESS`
- `EmuSize.cs` ✅ — `EmuSize ↔ System.Drawing.Size` 双向隐式转换包裹 `#if !HEADLESS`
- `EmuRectangle.cs` ✅ — `EmuRectangle ↔ System.Drawing.Rectangle` 双向隐式转换包裹 `#if !HEADLESS`
- `EmuRectangleF.cs` ✅ — `EmuRectangleF ↔ System.Drawing.RectangleF` 双向隐式转换包裹 `#if !HEADLESS`
- `EmuFont.cs` ✅ — `EmuFont ↔ System.Drawing.Font` 双向隐式转换 + 注释包裹 `#if !HEADLESS`
- `EmuFontStyle.cs` ✅ — `EmuFontStyle ↔ System.Drawing.FontStyle` 双向隐式转换 + 注释包裹 `#if !HEADLESS`

#### Headless 自有源码残留引用清理
- `ConsoleStateManager.cs` ✅ — 第 125 行 `System.Drawing.FontStyle.Regular` → `MinorShift.Emuera.Primitives.EmuFontStyle.Regular`（原代码依赖 `EmuFontStyle` 的隐式转换桥接 `System.Drawing.FontStyle`，移除 NuGet 后隐式转换被 `#if !HEADLESS` 隔离，必须直接用 Emu 类型）

#### NuGet 包移除
- `Emuera.Headless.csproj` ✅ — 删除 `<PackageReference Include="System.Drawing.Common" Version="10.0.9" />`，替换注释为「I-14 阶段 C：System.Drawing.Common 已彻底移除」说明

#### 验证
- `dotnet build` ✅ — 0 错误 0 警告
- `dotnet publish -c Release -o publish-test` ✅ — 成功，输出目录无任何 `System.Drawing.*.dll` 残留（`PublishSingleFile=true` 单文件发布）
- 冒烟测试 ✅ — `Emuera.Headless.exe --help` 正常输出（exit 0），证明运行时未因缺失 System.Drawing.Common 程序集而崩溃
- HEADLESS 自有代码 ✅ — 全量 grep `^\s*using\s+System\.Drawing` 仅命中 1 条 `using System.Drawing.Text.InstalledFontCollection ifc = new();`（位于 `Creator.Method.cs:2416` 的 `#else` 死代码块内，非 using 指令而是 using 语句，HEADLESS 构建不参与编译）

## 待办事项

### 5. 清理 using 语句（剩余零星项）

经全量扫描，`Emuera.Headless/**`（排除 `Shared/`）已无未包裹的 `using System.Drawing*`。`Shared/**` 下 19 条 using 指令 + 1 条 using 语句均已被正确包裹。

剩余可清理的零星 using（非阻塞 Step 6，但可顺手做）：
- `Process.ScriptProc.cs` — 第 13 行 using 可移除（代码已迁移到 EmuColor）
- `ConsoleButtonString.cs` — bitmapCache 已改为 IBitmapImage，using 可移除
- `ConfigItem.cs` — Step 4 后 `Color` 已无直接使用，第 6 行 using 可移除
- `ConfigData.cs` — Step 4 后 `Color` 已无直接使用，第 7 行 using 可移除
- `Config.cs` — Step 4 后 `Color`/`FontStyle` 已无直接使用，第 6 行 using 可移除

### 6. 移除 System.Drawing.Common ✅

以上所有步骤完成后：
1. ✅ 从 `Emuera.Headless.csproj` 删除 `<PackageReference Include="System.Drawing.Common" ... />`
2. ✅ 确认 `dotnet build` 0 错误（0 警告）
3. ✅ 确认 `dotnet publish` 成功（输出无 System.Drawing.*.dll）
4. ✅ 确认测试全部通过（无单元测试项目，改用冒烟测试 `Emuera.Headless.exe --help` exit 0）
5. ✅ 确认 Headless 自有代码无 `using System.Drawing`（仅 `#else` 死代码块内 1 处 using 语句）

## 执行顺序建议

1. ~~**Step 1**: DrawTo 签名重构~~ ✅
2. ~~**Step 2**: HeadlessStringMeasure 参数替换~~ ✅
3. ~~**Step 3**: GraphicsImage stub 与调用方对齐~~ ✅（用 `#if HEADLESS` 策略，非创建独立 HeadlessGraphicsImage）
4. ~~**Step 4**: Config 清理（ConfigItem<Color>→EmuColor，Config.cs 用法迁移）~~ ✅
5. ~~**Step 5**: 清理 using 语句~~ ✅（基本完成，剩余零星项非阻塞）
6. ~~**Step 6**: 移除 NuGet 引用（最终验证）~~ ✅

## 风险点

- ~~Shared/ 历史代码的 `#else`（WinForms）分支仍深度耦合 System.Drawing，HEADLESS 构建只编译 `#if HEADLESS` 分支，NuGet 移除后需确保 `#else` 不参与编译（当前 `DefineConstants=HEADLESS` 保证）~~ — 已确认所有 `#else` 分支均不参与 HEADLESS 编译
- ~~GraphicsImage stub 仍返回 `System.Drawing.Bitmap`（null）与 `System.Drawing.Rectangle`，Step 6 移除 NuGet 时需进一步抽象~~ — 已改为 `object Bitmap` 与 `EmuRectangle` 签名
- 未来引入 SKSharp 时，IImageContext/IBitmapImage 接口可能需要扩展
