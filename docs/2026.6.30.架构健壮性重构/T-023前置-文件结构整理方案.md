# T-023 前置：Emuera.Headless 文件结构整理方案

> 基准：[Emuera.Headless 剩余问题与行动方案.md](Emuera.Headless%20剩余问题与行动方案.md)
> 制定日期：2026-07-02
> 定位：T-023 物理迁移共享源码前的前置整理 PR，理顺 `Emuera.Headless/` 自有源码目录结构，为 T-023 的 `Shared/` 隔离方案预留边界。
> 维护范围：本项目**只维护 `Emuera.Headless`**，`Emuera/` WinForms 项目功能数年前已稳定，仅留作功能参考。

## 一、整理原则

1. **职责对齐**：物理目录与文件职责对齐，消除根目录扁平失序与 `Agent/` 混杂。
2. **namespace 不变**：物理移动不改动 namespace 声明与 using 引用，diff 最小、回滚容易。
3. **先整理自有源码**：本次 PR 只动 Headless 自有文件，T-023 作为独立 PR 处理共享源码迁移。
4. **为 T-023 预留边界**：本次产出 `Headless/`、`Terminal/` 目录边界，T-023 在此基础上新建 `Shared/` 隔离迁移源码。
5. **不动项**：`Server/`（T-024 处理 `ConsoleOutIO.cs`）、`UI/Game/Console/`、`UI/Game/EmueraConsole.cs`、`Properties/PublishProfiles/`。

## 二、决策记录

经与维护者讨论确认以下 7 项决策：

| # | 决策点 | 选择 | 理由 |
|---|---|---|---|
| 1 | 共享源码迁移后的目录边界 | 迁入 `Shared/` 子目录隔离 | T-022 可按路径分级启用 nullable + TreatWarningsAsErrors |
| 2 | 根目录散落文件归类 | 建立 `Headless/` 子目录放替换实现 | 与共享源码区分，根目录只留入口/全局类 |
| 3 | `Agent/` 目录拆分 | 拆分为 `Agent/` + `Terminal/` | 协议层与终端渲染职责分离 |
| 4 | 执行节奏 | 先整理自有源码，再做 T-023 | 两个 PR diff 各自聚焦，回滚边界清晰 |
| 5 | namespace 策略 | 保持不变 | 最小 diff、回滚容易、T-023 验收只要求 build 0 error |
| 6 | `EmueraConsole.cs` 归属 | 留在 `UI/Game/` | 与 `Console/` partial 拆分同生命周期，非替换实现 |
| 7 | `Terminal/` 目录位置 | 根级 `Terminal/` | 终端渲染独立于 UI 抽象层 |

## 三、目标目录结构

### 本次 PR 后的中间态

```
Emuera.Headless/
├── Agent/                       8 文件（协议层 + 按钮逻辑）
│   ├── AgentCliProtocol.cs
│   ├── AgentJsonlProtocol.cs
│   ├── AgentProtocolBase.cs
│   ├── AgentLog.cs
│   ├── JsonlCommand.cs
│   ├── IConsoleStateView.cs
│   ├── ButtonRegionTracker.cs
│   └── ButtonSelectionMode.cs
├── Headless/                    5 文件（Headless 替换实现）
│   ├── HeadlessClipboard.cs     ← 从根目录移入
│   ├── HeadlessDialog.cs        ← 从根目录移入
│   ├── HeadlessSound.cs         ← 从根目录移入
│   ├── HeadlessStringMeasure.cs ← 从根目录移入
│   └── HeadlessConsole.cs       ← 从 UI/Game/ 移入
├── Server/                       5 文件（不变，T-024 处理 ConsoleOutIO）
│   ├── ConsoleOutIO.cs
│   ├── HttpGameServer.cs
│   ├── HttpSessionIO.cs
│   ├── Session.cs
│   └── SessionIO.cs
├── Terminal/                     8 文件（从 Agent/ 拆出）
│   ├── AgentCliVtInput.cs
│   ├── AgentCliVtScreen.cs
│   ├── CountdownRenderer.cs
│   ├── TerminalCursor.cs
│   ├── TerminalDisplayWidth.cs
│   ├── TerminalRenderer.cs
│   ├── VtParser.cs
│   └── Win32ConsoleInterop.cs
├── UI/
│   ├── Game/
│   │   ├── Console/             6 文件（不变）
│   │   │   ├── ConsoleInputHandler.cs
│   │   │   ├── ConsolePrintManager.cs
│   │   │   ├── ConsoleRefreshHandler.cs
│   │   │   ├── ConsoleStateData.cs
│   │   │   ├── ConsoleStateManager.cs
│   │   │   └── ConsoleTimerManager.cs
│   │   └── EmueraConsole.cs      （留原处，非替换实现）
│   └── WindowsConsoleHelper.cs
├── Properties/
│   └── PublishProfiles/
│       └── FolderProfile.pubxml
├── Emuera.Headless.csproj
└── 根目录 7 个入口/全局类：
    Program.cs, HeadlessRunner.cs, ServerRunner.cs, HeadlessOptions.cs,
    GlobalStatic.cs, GameExitException.cs, GamePaths.cs
```

### T-023 后的最终态（规划，本次不执行）

```
Emuera.Headless/
├── Agent/                       8 文件
├── Headless/                    5 文件
├── Server/                      4 文件（T-024 后）
├── Terminal/                    8 文件
├── UI/
│   ├── Game/
│   │   ├── Console/             6 文件
│   │   └── EmueraConsole.cs
│   └── WindowsConsoleHelper.cs
├── Shared/                      ← T-023 迁移目标（隔离历史源码）
│   ├── Runtime/                 ← 排除 Sound.WMP/NAudio/WinInput/Clipboard
│   ├── UI/
│   │   ├── Game/                ← 共享 UI/Game 文件（不覆盖 EmueraConsole.cs）
│   │   └── FontFactory.cs
│   └── Properties/lang/
├── Properties/PublishProfiles/
└── 根目录入口文件
```

`Shared/` 边界让 T-022 可按路径分级：`[Emuera.Headless/Shared/**]` 抑制历史警告，`[Emuera.Headless/**]`（排除 Shared）启用 nullable + TreatWarningsAsErrors。

## 四、本次 PR 执行清单

### 文件移动（13 个文件，全部用 `git mv` 保留历史）

| # | 源路径 | 目标路径 |
|---|---|---|
| 1 | `Emuera.Headless/HeadlessClipboard.cs` | `Emuera.Headless/Headless/HeadlessClipboard.cs` |
| 2 | `Emuera.Headless/HeadlessDialog.cs` | `Emuera.Headless/Headless/HeadlessDialog.cs` |
| 3 | `Emuera.Headless/HeadlessSound.cs` | `Emuera.Headless/Headless/HeadlessSound.cs` |
| 4 | `Emuera.Headless/HeadlessStringMeasure.cs` | `Emuera.Headless/Headless/HeadlessStringMeasure.cs` |
| 5 | `Emuera.Headless/UI/Game/HeadlessConsole.cs` | `Emuera.Headless/Headless/HeadlessConsole.cs` |
| 6 | `Emuera.Headless/Agent/AgentCliVtInput.cs` | `Emuera.Headless/Terminal/AgentCliVtInput.cs` |
| 7 | `Emuera.Headless/Agent/AgentCliVtScreen.cs` | `Emuera.Headless/Terminal/AgentCliVtScreen.cs` |
| 8 | `Emuera.Headless/Agent/CountdownRenderer.cs` | `Emuera.Headless/Terminal/CountdownRenderer.cs` |
| 9 | `Emuera.Headless/Agent/TerminalCursor.cs` | `Emuera.Headless/Terminal/TerminalCursor.cs` |
| 10 | `Emuera.Headless/Agent/TerminalDisplayWidth.cs` | `Emuera.Headless/Terminal/TerminalDisplayWidth.cs` |
| 11 | `Emuera.Headless/Agent/TerminalRenderer.cs` | `Emuera.Headless/Terminal/TerminalRenderer.cs` |
| 12 | `Emuera.Headless/Agent/VtParser.cs` | `Emuera.Headless/Terminal/VtParser.cs` |
| 13 | `Emuera.Headless/Agent/Win32ConsoleInterop.cs` | `Emuera.Headless/Terminal/Win32ConsoleInterop.cs` |

### csproj 调整（仅 1 处注释更新）

[`Emuera.Headless.csproj:42`](../../Emuera.Headless/Emuera.Headless.csproj#L42) 注释更新：

```diff
- <!-- HeadlessConsole.cs: 已移至 Emuera.Headless/UI/Game/，由 SDK 默认 glob 包含（I-11） -->
+ <!-- HeadlessConsole.cs: 已移至 Emuera.Headless/Headless/，由 SDK 默认 glob 包含 -->
```

无其他 csproj 改动——13 个文件原本都由 SDK 默认 glob 包含，移动后仍由 SDK 默认 glob 包含。

### .editorconfig 调整

**本次不动**。`Shared/` 目录尚未创建，预先添加 `[Emuera.Headless/Shared/**]` 规则无意义。T-023 PR 中同步调整：

- 删除 `[Emuera/**]` 段
- 新增 `[Emuera.Headless/Shared/**]` 段（继承原 `[Emuera/**]` 的抑制规则）

## 五、namespace 策略

- 13 个移动文件 **namespace 声明保持不变**
- 不改动任何 `using` 引用
- 代价：物理目录与 namespace 不完全一致（如 `HeadlessClipboard.cs` 在 `Headless/` 目录但 namespace 仍为 `MinorShift.Emuera.Misc`）
- 收益：diff 最小、回滚容易、T-023 验收只要求 build 0 error

长期一致性可在 T-023 后统一评估（涉及大面积 using 改动，需独立 PR）。

## 六、验收标准

1. `dotnet build Emuera.Headless/Emuera.Headless.csproj -c Debug` 0 error
2. warning 基线不变（约 140 条，移动文件不引入新警告）
3. `run_all.py --binary .../Emuera.Headless.exe --game-dir test_game` 全部回归测试通过
4. `git diff --stat` 显示纯文件移动（rename 检测率 100%），无逻辑改动
5. 检查 `obj/Debug/net10.0/Emuera.Headless.csproj.FileListAbsolute.txt` 确认 13 个文件在新位置被正确包含

## 七、风险与回滚

| 风险 | 影响 | 缓解 |
|---|---|---|
| SDK 默认 glob 未包含新位置文件 | build 失败 | 验收标准 5 检查 FileListAbsolute.txt |
| `HeadlessConsole.cs` 移动后 IConsoleUI 实现链接断裂 | build 失败 | namespace 不变，链接不断裂；build 验证 |
| 测试夹具依赖原路径 | 回归测试失败 | `tests/` 不引用源码路径，只引用 binary；run_all.py 验证 |
| 文档链接失效 | 仓库内 .md 引用旧路径 | 本次整理同步更新 CLAUDE.md / TODO.md 路径引用；T-023 后统一刷新文档链接 |

**回滚**：`git revert` 即可，无逻辑改动、无 csproj 结构性变更。

## 八、与 T-023 的衔接

| 本次 PR 产出 | T-023 PR 复用 |
|---|---|
| `Headless/` 目录边界 | 不冲突，T-023 只动 `Shared/` |
| `Terminal/` 目录边界 | 不冲突 |
| `EmueraConsole.cs` 留 `UI/Game/` | T-023 文档"不可覆盖"约束已满足 |
| `HeadlessConsole.cs` 在 `Headless/` | T-023 不再需要"HeadlessConsole 已移至 UI/Game/"的特殊注释 |
| namespace 不变 | T-023 迁移的共享源码也保持原 namespace，策略一致 |

T-023 PR 的迁移目标路径需相应调整（相对原方案）：

| 原方案 | 调整后 |
|---|---|
| `git mv Emuera/Runtime/ Emuera.Headless/Runtime/` | `git mv Emuera/Runtime/ Emuera.Headless/Shared/Runtime/` |
| `git mv Emuera/UI/Game/<file> Emuera.Headless/UI/Game/<file>` | `git mv Emuera/UI/Game/<file> Emuera.Headless/Shared/UI/Game/<file>` |
| `git mv Emuera/UI/FontFactory.cs Emuera.Headless/UI/FontFactory.cs` | `git mv Emuera/UI/FontFactory.cs Emuera.Headless/Shared/UI/FontFactory.cs` |
| `git mv Emuera/Properties/lang/ Emuera.Headless/Properties/lang/` | `git mv Emuera/Properties/lang/ Emuera.Headless/Shared/Properties/lang/` |

## 九、执行顺序建议

1. **本次 PR（T-025）**：13 个文件 `git mv` + csproj 注释更新 + CLAUDE.md/TODO.md 路径同步
2. **T-023 PR**：新建 `Shared/` 子树，物理迁移 `Emuera/` 共享源码，删除 csproj glob，删除 `Emuera/Emuera.csproj` 与 `Emuera/Emuera.sln`
3. **T-024 PR**：删除 `Server/ConsoleOutIO.cs`，移除 stdin 管道分支
4. **T-022 PR**：`.editorconfig` 调整 `[Emuera/**]` → `[Emuera.Headless/Shared/**]`，启用 nullable + TreatWarningsAsErrors

每个 PR diff 聚焦、回滚边界清晰，互不阻塞。
